export const metadata = {
    title: "Breach Lab",
    description:
        "Scanner vos mots de passe contre les fuites connues et verifier un mot personnalise en direct.",
    robots: {
        index: false,
        follow: false,
    },
};

export default function BreachLabLayout({ children }) {
    return children;
}
