export const metadata = {
    title: "Configuration de la sécurité",
    description: "Configurez la double authentification, le code PIN et la biométrie pour sécuriser votre compte.",
};

export default function OnboardingLayout({ children }) {
    return children;
}
